import { useEffect, useState } from 'react';
import { useParams } from 'react-router-dom';
import { api } from '../api/client';
import { RadarScores, TopicChart } from '../components/charts/ScoreCharts';
export default function Report(){const {id}=useParams();const [report,setReport]=useState(null);useEffect(()=>{api.get(`/interviews/${id}/report`).then(r=>setReport(r.data.report));},[id]);if(!report)return 'Generating report...';return <div className="space-y-6"><h1 className="text-4xl font-bold">Final Interview Report</h1><div className="grid gap-4 md:grid-cols-4">{['overallScore','technicalScore','communicationScore','behavioralScore'].map(k=><div className="card" key={k}><p className="text-slate-400">{k}</p><p className="text-3xl font-bold">{report[k] ?? 0}/10</p></div>)}</div><div className="grid gap-6 lg:grid-cols-2"><section className="card"><h2 className="text-xl font-bold">Radar</h2><RadarScores data={report.radar}/></section><section className="card"><h2 className="text-xl font-bold">Topics</h2><TopicChart data={report.topicBreakdown}/></section></div><section className="card"><h2 className="text-xl font-bold">Recommendations</h2><ul className="list-disc pl-6">{report.recommendations?.map((r,i)=><li key={i}>{r}</li>)}</ul><p className="mt-4">Weakest: {report.weakestTopics?.join(', ')}</p><p>Strongest: {report.strongestTopics?.join(', ')}</p></section></div>}
